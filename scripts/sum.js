function sum(){
    //getting a
    let a=prompt("Enter the first value (a):");
    //getting b
    let b=prompt("Enter the second value (b):");
    //sum
    let total;
    total=Number(a)+ Number(b);
    //display the results
    console.log(total);
}

function mult(){
    let a=prompt("Enter the first value (a):");
    let b=5;
    totalMult = Number(a) * Number(b);
    console.log(totalMult);
}